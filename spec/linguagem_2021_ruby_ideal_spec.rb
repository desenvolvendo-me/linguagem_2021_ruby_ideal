# frozen_string_literal: true

RSpec.describe Linguagem2021RubyIdeal do
  it "has a version number" do
    expect(Linguagem2021RubyIdeal::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
